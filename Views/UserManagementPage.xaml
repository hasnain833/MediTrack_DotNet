<Page
    x:Class="MediTrack.Views.UserManagementPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:vm="using:MediTrack.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource AppBackgroundBrush}" Padding="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="User Administration" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,24"/>

        <Border Grid.Row="1" Style="{StaticResource PremiumCardStyle}" Margin="0,0,0,24">
            <StackPanel Orientation="Horizontal" Spacing="16">
                <Button Content="Add New User" Style="{StaticResource PremiumButtonStyle}" Width="180"/>
                <Button Command="{x:Bind ViewModel.RefreshCommand, Mode=OneWay}" 
                        Style="{StaticResource TextBlockButtonStyle}" Padding="12,8">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <FontIcon Glyph="&#xE72C;" FontSize="16" Foreground="{StaticResource AccessKeySecondaryForeground}"/>
                        <TextBlock Text="Refresh List" VerticalAlignment="Center" FontWeight="Medium"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource PremiumCardStyle}" Padding="0">
            <controls:DataGrid AutoGenerateColumns="False"
                               ItemsSource="{x:Bind ViewModel.Users}"
                               GridLinesVisibility="Horizontal" HeadersVisibility="All"
                               RowHeight="54" BorderThickness="0" Background="Transparent">
                <controls:DataGrid.Columns>
                    <controls:DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                    <controls:DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="*"/>
                    <controls:DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="150"/>
                    <controls:DataGridTemplateColumn Header="Status" Width="120">
                        <controls:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="#EFF6FF" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left" Margin="12,0">
                                    <TextBlock Text="{Binding Status}" Foreground="#2563EB" FontSize="12" FontWeight="SemiBold"/>
                                </Border>
                            </DataTemplate>
                        </controls:DataGridTemplateColumn.CellTemplate>
                    </controls:DataGridTemplateColumn>
                </controls:DataGrid.Columns>
            </controls:DataGrid>
        </Border>

        <ProgressRing Grid.Row="2" IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}" 
                       VerticalAlignment="Center" HorizontalAlignment="Center"
                       Foreground="{StaticResource AccentAccent}" Width="40" Height="40"/>
    </Grid>
</Page>
