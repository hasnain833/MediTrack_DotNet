<Page
    x:Class="MediTrack.Views.UserManagementPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:vm="using:MediTrack.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="User Administration" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,24"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12" Margin="0,0,0,24">
            <Button Content="Add New User" Style="{StaticResource PremiumButtonStyle}"/>
            <Button Content="Roles &amp; Permissions" Background="Transparent" BorderBrush="#E0E0E0"/>
        </StackPanel>

        <controls:DataGrid Grid.Row="2" AutoGenerateColumns="False"
                           ItemsSource="{x:Bind ViewModel.Users}"
                           GridLinesVisibility="Horizontal" HeadersVisibility="All"
                           RowHeight="48" BorderThickness="0">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                <controls:DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="*"/>
                <controls:DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="150"/>
                <controls:DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>
            </controls:DataGrid.Columns>
        </controls:DataGrid>
    </Grid>
</Page>
