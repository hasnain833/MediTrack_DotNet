<Page
    x:Class="DChemist.Views.FinancialPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:vm="using:DChemist.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource AppBackgroundBrush}" Padding="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <!-- Revenue Stats -->
        <ItemsControl Grid.Row="1" ItemsSource="{x:Bind ViewModel.RevenueStats}" Margin="0,0,0,32">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="24"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="vm:RevenueStat">
                    <Border Width="220" Style="{StaticResource PremiumCardStyle}">
                        <StackPanel Spacing="8">
                            <TextBlock Text="{x:Bind Label}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="13" FontWeight="Medium"/>
                            <TextBlock Text="{x:Bind Value}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                            <TextBlock Text="{x:Bind Change}" Foreground="#10B981" FontSize="12" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Sales History -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <StackPanel Orientation="Horizontal" Spacing="16" VerticalAlignment="Center" Margin="0,0,0,20">
                <TextBlock Text="Recent Transactions" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                <Button Style="{StaticResource AccentButtonStyle}" Command="{x:Bind ViewModel.ExportCommand}" VerticalAlignment="Center" Padding="12,6">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE896;" FontSize="14"/>
                        <TextBlock Text="Export to Excel" Margin="0,0,0,1"/>
                    </StackPanel>
                </Button>
            </StackPanel>

            <Border Grid.Row="1" Style="{StaticResource PremiumCardStyle}" Padding="0">
                <controls:DataGrid AutoGenerateColumns="False"
                                   ItemsSource="{x:Bind ViewModel.SalesHistory}"
                                   GridLinesVisibility="Horizontal" HeadersVisibility="All"
                                   RowHeight="54" BorderThickness="0" Background="Transparent">
                    <controls:DataGrid.Columns>
                        <controls:DataGridTextColumn Header="Bill No" Binding="{Binding BillNo}" Width="150"/>
                        <controls:DataGridTextColumn Header="Customer" Binding="{Binding Customer}" Width="*"/>
                        <controls:DataGridTextColumn Header="Amount" Binding="{Binding Amount, Converter={StaticResource CurrencyConverter}}" Width="120"/>
                        <controls:DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="180"/>
                        <controls:DataGridTemplateColumn Header="Status" Width="120">
                            <controls:DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="#ECFDF5" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left" Margin="12,0">
                                        <TextBlock Text="{Binding Status}" Foreground="#059669" FontSize="12" FontWeight="SemiBold"/>
                                    </Border>
                                </DataTemplate>
                            </controls:DataGridTemplateColumn.CellTemplate>
                        </controls:DataGridTemplateColumn>
                    </controls:DataGrid.Columns>
                </controls:DataGrid>
            </Border>
        </Grid>
    </Grid>
</Page>
