<Page
    x:Class="MediTrack.Views.FinancialPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:vm="using:MediTrack.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Financial Overview" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,24"/>

        <!-- Revenue Stats -->
        <ItemsControl Grid.Row="1" ItemsSource="{x:Bind ViewModel.RevenueStats}" Margin="0,0,0,32">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="24"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="vm:RevenueStat">
                    <Border Width="200" Background="#F9F9F9" CornerRadius="12" Padding="20" BorderThickness="1" BorderBrush="#E0E0E0">
                        <StackPanel Spacing="4">
                            <TextBlock Text="{x:Bind Label}" Foreground="#666666" FontSize="12"/>
                            <TextBlock Text="{x:Bind Value}" FontSize="20" FontWeight="Bold"/>
                            <TextBlock Text="{x:Bind Change}" Foreground="#2ECC71" FontSize="11" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Sales History -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <TextBlock Text="Recent Transactions" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,16"/>

            <controls:DataGrid Grid.Row="1" AutoGenerateColumns="False"
                               ItemsSource="{x:Bind ViewModel.SalesHistory}"
                               GridLinesVisibility="Horizontal" HeadersVisibility="All"
                               RowHeight="48" BorderThickness="0">
                <controls:DataGrid.Columns>
                    <controls:DataGridTextColumn Header="Bill No" Binding="{Binding BillNo}" Width="150"/>
                    <controls:DataGridTextColumn Header="Customer" Binding="{Binding Customer}" Width="*"/>
                    <controls:DataGridTextColumn Header="Amount" Binding="{Binding Amount, Converter={StaticResource CurrencyConverter}}" Width="120"/>
                    <controls:DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="150"/>
                    <controls:DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                </controls:DataGrid.Columns>
            </controls:DataGrid>
        </Grid>
    </Grid>
</Page>
