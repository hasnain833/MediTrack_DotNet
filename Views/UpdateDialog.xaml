<ContentDialog
    x:Class="DChemist.Views.UpdateDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="New Version Available"
    PrimaryButtonText="Update Now"
    SecondaryButtonText="Remind Me Later"
    DefaultButton="Primary"
    Style="{StaticResource DefaultContentDialogStyle}">

    <StackPanel Spacing="20" Width="400" Margin="0,10,0,0">
        <StackPanel Spacing="4">
            <TextBlock Text="A new version of D. Chemist is ready to install!" 
                       FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
            <TextBlock x:Name="VersionText" Text="Version 1.1.0" 
                       FontSize="14" Foreground="{StaticResource AccentAccent}" FontWeight="Bold"/>
        </StackPanel>

        <Border Background="#F9F9F9" CornerRadius="8" Padding="12" BorderBrush="#EEEEEE" BorderThickness="1">
            <StackPanel Spacing="8">
                <TextBlock Text="What's New:" FontWeight="SemiBold" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                <TextBlock x:Name="ReleaseNotesText" TextWrapping="Wrap" FontSize="13" 
                           Foreground="{StaticResource TextPrimaryBrush}" Height="120"/>
            </StackPanel>
        </Border>

        <StackPanel x:Name="ProgressContainer" Visibility="Collapsed" Spacing="10">
            <TextBlock Text="Downloading update..." FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
            <ProgressBar x:Name="UpdateProgressBar" Height="8" Minimum="0" Maximum="100" Value="0"/>
            <TextBlock x:Name="ProgressText" Text="0%" HorizontalAlignment="Right" FontSize="10" Foreground="{StaticResource AccentAccent}"/>
        </StackPanel>

        <TextBlock x:Name="ErrorText" Foreground="#E53E3E" FontSize="12" Visibility="Collapsed" TextWrapping="Wrap"/>
    </StackPanel>
</ContentDialog>
