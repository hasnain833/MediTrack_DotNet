<Page
    x:Class="MediTrack.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MediTrack.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource PremiumBackgroundBrush}">
        <Border Width="450" VerticalAlignment="Center" HorizontalAlignment="Center"
                Style="{StaticResource GlassBorderStyle}" Padding="40" Margin="20">
            <StackPanel Spacing="24">
                <TextBlock Text="MediTrack" FontSize="42" FontWeight="Bold" 
                           Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Text="Secure Login" FontSize="18" Foreground="#CCFFFFFF" 
                           HorizontalAlignment="Center" Margin="0,-10,0,10"/>

                <TextBox Header="Username" Text="{x:Bind ViewModel.Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Foreground="White" PlaceholderText="Enter username"/>
                
                <PasswordBox Header="Password" Password="{x:Bind ViewModel.Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             Foreground="White" PlaceholderText="Enter password"/>

                <TextBlock Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}" 
                           Foreground="#FF4444" 
                           Visibility="{x:Bind ViewModel.ErrorMessage, Converter={StaticResource StringToVisibilityConverter}, Mode=OneWay}"
                           TextWrapping="Wrap"/>

                <Button Content="LOGIN" Command="{x:Bind ViewModel.LoginCommand}" 
                        HorizontalAlignment="Stretch" Style="{StaticResource PremiumButtonStyle}"/>

                <ProgressRing IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}" Foreground="{StaticResource AccentAccent}"
                              HorizontalAlignment="Center" Width="40" Height="40"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
