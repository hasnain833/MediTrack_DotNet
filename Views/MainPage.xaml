<Page
    x:Class="MediTrack.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MediTrack.Views"
    xmlns:vm="using:MediTrack.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:MediTrack.Models"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource AppBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="{ThemeResource SidebarBackgroundBrush}" Padding="0,20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Spacing="8" Margin="24,20,24,40">
                    <TextBlock Text="D. Chemist" FontSize="24" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Management System" FontSize="12" Foreground="#88FFFFFF"/>
                </StackPanel>

                <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.NavigationItems}"
                          SelectedItem="{x:Bind ViewModel.SelectedItem, Mode=TwoWay}"
                          SelectionMode="Single" Padding="12,0"
                          ItemContainerStyle="{StaticResource SidebarItemStyle}">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:NavigationItem">
                            <Grid Height="44" Margin="0,2">
                                <StackPanel Orientation="Horizontal" Spacing="16" VerticalAlignment="Center" Margin="12,0">
                                    <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="{x:Bind Icon}" 
                                              FontSize="18" Foreground="Inherit"/>
                                    <TextBlock Text="{x:Bind Title}" FontSize="14" FontWeight="Medium" Foreground="Inherit"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <StackPanel Grid.Row="2" Margin="24,20" Spacing="16">
                    <Border Height="1" Background="#15FFFFFF" HorizontalAlignment="Stretch"/>
                    <Grid>
                        <StackPanel Spacing="2">
                            <TextBlock Text="{x:Bind ViewModel.CurrentUser.FullName, Mode=OneWay}" 
                                       Foreground="White" FontWeight="SemiBold" FontSize="14"/>
                            <TextBlock Text="{x:Bind ViewModel.CurrentUser.Role, Mode=OneWay}" 
                                       Foreground="#88FFFFFF" FontSize="11"/>
                        </StackPanel>
                        <Button HorizontalAlignment="Right" Command="{x:Bind ViewModel.LogoutCommand}"
                                Style="{StaticResource TextBlockButtonStyle}" VerticalAlignment="Center">
                            <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE8BB;" Foreground="White" FontSize="18"/>
                        </Button>
                    </Grid>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content Area -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="White" Padding="32,0">
                <Grid.Shadow>
                    <ThemeShadow />
                </Grid.Shadow>
                <TextBlock Text="{x:Bind ViewModel.Title, Mode=OneWay}" 
                           FontSize="20" FontWeight="Bold" VerticalAlignment="Center"
                           Foreground="{StaticResource TextPrimaryBrush}"/>
            </Grid>

            <Frame x:Name="ContentFrame" Grid.Row="1" Padding="32"/>
        </Grid>
    </Grid>
</Page>
